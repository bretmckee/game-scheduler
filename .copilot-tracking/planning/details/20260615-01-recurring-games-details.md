<!-- markdownlint-disable-file -->

# Task Details: Recurring Games

## Research Reference

**Source Research**: #file:../research/20260615-01-recurring-games-research.md

---

## Phase 1: DB Migration + Model + API Schemas + Clone Propagation

### Task 1.1: Alembic migration for `recur_rule` column

Generate and implement an Alembic migration to add `recur_rule VARCHAR(200) NULL` to `game_sessions`.

- **Files**:
  - `alembic/versions/XXXX_add_recur_rule_to_game_sessions.py` — new migration (autogenerated name)
- **Steps**:
  - `uv run alembic revision --autogenerate -m "add recur_rule to game_sessions"` (review generated file)
  - Verify upgrade adds `op.add_column('game_sessions', sa.Column('recur_rule', sa.String(200), nullable=True))`
  - Verify downgrade removes the column with `op.drop_column('game_sessions', 'recur_rule')`
- **Success**:
  - `uv run alembic upgrade head` completes without error
  - `uv run alembic downgrade -1` completes without error
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 186-189) — schema recommendation
- **Dependencies**:
  - None

### Task 1.2: Update `GameSession` model

Add `recur_rule: Mapped[str | None]` to the `GameSession` SQLAlchemy model in `shared/models/game.py`.

- **Files**:
  - `shared/models/game.py` — add field to `GameSession`
- **Implementation**:
  ```python
  recur_rule: Mapped[str | None] = mapped_column(String(200), nullable=True)
  ```
  Place after `post_at` field.
- **Success**:
  - `uv run mypy shared/` passes
  - `python -c "from shared.models.game import GameSession; print(GameSession.recur_rule)"` does not raise
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 53-55) — `GameSession` fields and placement
- **Dependencies**:
  - Task 1.1 complete

### Task 1.3: Add `recur_rule` to API schemas

Expose `recur_rule` in create, update, and response schemas.

- **Files**:
  - `services/api/schemas/game.py` (or wherever `GameCreateRequest`, `GameUpdateRequest`, `GameResponse` are defined) — add `recur_rule: str | None = None` field to each
- **Implementation**:
  ```python
  recur_rule: str | None = None
  ```
  Add to `GameCreateRequest`, `GameUpdateRequest`, and `GameResponse`.
- **Success**:
  - `uv run mypy services/api/` passes
  - `GET /api/v1/games/{id}` response includes `recur_rule` field (null if unset)
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 242-244) — expose in schemas
- **Dependencies**:
  - Task 1.2 complete

### Task 1.4: Propagate `recur_rule` through `clone_game()` and write unit test

In `services/api/services/games.py`, copy `source.recur_rule` to the cloned `GameSession`. Write the unit test immediately (retrofitting pattern — the implementation and test land in the same commit).

- **Files**:
  - `services/api/services/games.py` — copy `recur_rule` in `clone_game()` (or `_build_cloned_game()`)
  - `tests/unit/services/test_clone_game.py` — add `test_clone_game_propagates_recur_rule`
- **Implementation** (locate where the clone object is constructed and add):
  ```python
  clone.recur_rule = source.recur_rule
  ```
- **Test**:
  ```python
  def test_clone_game_propagates_recur_rule(...):
      source.recur_rule = "FREQ=WEEKLY;BYDAY=SA"
      result = clone_game(...)
      assert result.recur_rule == "FREQ=WEEKLY;BYDAY=SA"
  ```
- **Success**:
  - `uv run pytest tests/unit/services/test_clone_game.py -v` — new test passes
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 243, 290-292) — `clone_game()` propagation
- **Dependencies**:
  - Task 1.3 complete

---

## Phase 2: DM Format + `RecurrenceConfirmationView` Stubs + RED Unit Tests

### Task 2.1: Add `DMFormats.recurrence_confirmation` stub

Add a `NotImplementedError` stub so Phase 2 tests can import the name.

- **Files**:
  - `shared/message_formats.py` — add stub after `clone_confirmation` method in `DMFormats`
- **Implementation**:
  ```python
  @staticmethod
  def recurrence_confirmation(game_title: str, next_at_unix: int) -> str:
      raise NotImplementedError
  ```
- **Success**:
  - `python -c "from shared.message_formats import DMFormats; DMFormats.recurrence_confirmation"` does not raise `AttributeError`
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 211-220) — `DMFormats.recurrence_confirmation` spec
- **Dependencies**:
  - None (can land before Phase 1)

### Task 2.2: Add `RecurrenceConfirmationView` stub file

Create a new view file with a stub class so Phase 2 tests can import it.

- **Files**:
  - `services/bot/views/recurrence_confirmation_view.py` — new file
- **Implementation**:

  ```python
  import discord

  class RecurrenceConfirmationView(discord.ui.View):
      async def confirm(self, interaction: discord.Interaction) -> None:
          raise NotImplementedError

      async def decline(self, interaction: discord.Interaction) -> None:
          raise NotImplementedError
  ```

- **Success**:
  - `python -c "from services.bot.views.recurrence_confirmation_view import RecurrenceConfirmationView"` does not raise
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 222-228) — view design
- **Dependencies**:
  - None

### Task 2.3: Add xfail unit tests for `DMFormats.recurrence_confirmation`

Append to `tests/unit/shared/test_message_formats.py`.

- **Files**:
  - `tests/unit/shared/test_message_formats.py` — append tests
- **Tests** (all `@pytest.mark.xfail(strict=True, reason="...")`):
  - `test_recurrence_confirmation_contains_title` — format string includes game title
  - `test_recurrence_confirmation_contains_discord_timestamp` — includes `<t:{next_at_unix}:F>`
  - `test_recurrence_confirmation_mentions_confirmation_action` — contains "Confirm" or "confirm"
- **Success**:
  - `uv run pytest tests/unit/shared/test_message_formats.py -v` — new tests show as `xfailed`
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 211-220) — exact DM text template
- **Dependencies**:
  - Task 2.1 complete

### Task 2.4: Add xfail unit tests for `RecurrenceConfirmationView`

Create `tests/unit/services/bot/views/test_recurrence_confirmation_view.py`.

- **Files**:
  - `tests/unit/services/bot/views/test_recurrence_confirmation_view.py` — new file
- **Tests** (all `@pytest.mark.xfail(strict=True, reason="...")`):
  - `test_confirm_sets_post_at_to_now` — Confirm callback sets `game.post_at` to approximately now
  - `test_confirm_sends_pg_notify` — Confirm callback triggers `NOTIFY game_announcement_changed`
  - `test_decline_cancels_game` — Decline callback cancels the game (sets status to CANCELLED)
- **Success**:
  - `uv run pytest tests/unit/services/bot/views/test_recurrence_confirmation_view.py -v` — all `xfailed`
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 222-228, 328-333) — `RecurrenceConfirmationView` buttons spec
- **Dependencies**:
  - Task 2.2 complete

---

## Phase 3: DM Format + `RecurrenceConfirmationView` GREEN

### Task 3.1: Implement `DMFormats.recurrence_confirmation`

Replace the stub with the real implementation.

- **Files**:
  - `shared/message_formats.py` — replace stub in `DMFormats`
- **Implementation**:
  ```python
  @staticmethod
  def recurrence_confirmation(game_title: str, next_at_unix: int) -> str:
      return (
          f"🔁 **{game_title}** has ended! The next session is scheduled for "
          f"<t:{next_at_unix}:F>.\n\n"
          "Confirm to announce it to players, or decline to cancel it. "
          "If you don't respond before the game starts, it will be automatically cancelled."
      )
  ```
- **Success**:
  - Phase 2 xfail tests for `DMFormats.recurrence_confirmation` now pass (remove `xfail` after confirming)
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 211-219) — exact DM text
- **Dependencies**:
  - Task 2.1 complete

### Task 3.2: Implement `RecurrenceConfirmationView`

Replace the stub with the full implementation following `CloneConfirmationView` as a pattern.

- **Files**:
  - `services/bot/views/recurrence_confirmation_view.py` — replace stub with full implementation
- **Behaviour**:
  - Constructor accepts `game_id: str`, `db_factory`, and a reference to the bot session
  - **Confirm button**: loads game, sets `game.post_at = utc_now()`, commits, sends `NOTIFY game_announcement_changed`, edits interaction response to acknowledge
  - **Decline button**: loads game, cancels game (sets status to CANCELLED), commits, edits interaction response
  - Study `services/bot/views/clone_confirmation_view.py` for exact patterns (DB access, notify, error handling)
- **Success**:
  - Phase 2 xfail tests for `RecurrenceConfirmationView` now pass (remove `xfail` after confirming)
  - `uv run mypy services/bot/views/` passes
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 222-228) — view spec
  - #file:../research/20260615-01-recurring-games-research.md (Lines 100-108) — API confirmation path and NOTIFY mechanism
- **Dependencies**:
  - Task 2.2 complete; Task 3.1 complete

### Task 3.3: Remove xfail markers from Phase 2 tests

- **Files**:
  - `tests/unit/shared/test_message_formats.py` — remove `@pytest.mark.xfail` from Phase 2 tests
  - `tests/unit/services/bot/views/test_recurrence_confirmation_view.py` — remove `@pytest.mark.xfail` from all tests
- **Success**:
  - `uv run pytest tests/unit/shared/test_message_formats.py tests/unit/services/bot/views/test_recurrence_confirmation_view.py -v` — all PASSED
- **Dependencies**:
  - Tasks 3.1 and 3.2 complete

---

## Phase 4: `_system_clone_for_recurrence` Stub + RED Unit Tests

### Task 4.1: Add `_system_clone_for_recurrence` stub to `GameService`

Add a `NotImplementedError` stub in `services/api/services/games.py` so Phase 4 tests can import and reference the method.

- **Files**:
  - `services/api/services/games.py` — add stub method to `GameService`
- **Implementation**:
  ```python
  async def _system_clone_for_recurrence(
      self,
      db: AsyncSession,
      source: GameSession,
      next_at: datetime,
  ) -> GameSession:
      raise NotImplementedError
  ```
- **Success**:
  - `uv run mypy services/api/services/games.py` passes
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 196-210) — full spec for `_system_clone_for_recurrence`
- **Dependencies**:
  - Phase 1 complete (needs `recur_rule` field on `GameSession`)

### Task 4.2: Add xfail unit tests for `_system_clone_for_recurrence`

Create `tests/unit/services/test_system_clone_for_recurrence.py`.

- **Files**:
  - `tests/unit/services/test_system_clone_for_recurrence.py` — new test file
- **Tests** (all `@pytest.mark.xfail(strict=True, reason="...")`):
  - `test_system_clone_sets_post_at_none` — clone has `post_at=None`
  - `test_system_clone_copies_recur_rule` — clone inherits `source.recur_rule`
  - `test_system_clone_carries_over_confirmed_players` — confirmed participants are copied to clone
  - `test_system_clone_does_not_publish_game_created` — `_publish_game_created` NOT called
  - `test_system_clone_creates_status_schedules` — IN_PROGRESS + COMPLETED `GameStatusSchedule` rows created
  - `test_system_clone_sets_scheduled_at_to_next_at` — clone `scheduled_at` equals provided `next_at`
- **Success**:
  - `uv run pytest tests/unit/services/test_system_clone_for_recurrence.py -v` — all `xfailed`
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 196-210, 289-305) — spec and test list
- **Dependencies**:
  - Task 4.1 complete

---

## Phase 5: `_system_clone_for_recurrence` GREEN

### Task 5.1: Implement `_system_clone_for_recurrence`

Replace the stub with a full implementation in `services/api/services/games.py`.

- **Files**:
  - `services/api/services/games.py` — replace stub in `GameService`
- **Implementation details**:
  - Copy all relevant source fields: `title`, `description`, `scheduled_at=next_at`, `duration_minutes`, `max_players`, `channel_id`, `host_id`, `signup_method`, `signup_deadline`, `post_at=None`, `message_id=None`, `recur_rule`, `remind_host_rewards`, `archive_delay_seconds`, etc.
  - Set `status = GameStatus.SCHEDULED.value`, `rewards = None`
  - Call `_create_game_status_schedules(db, clone)` (schedules IN_PROGRESS + COMPLETED transitions)
  - Do **NOT** call `_setup_game_schedules()` (reminders deferred until announcement)
  - Do **NOT** call `_publish_game_created()` (no Discord event yet)
  - Carry over confirmed participants (carryover=YES): copy `Participant` rows with `position_type=CONFIRMED` from source to clone
  - Does NOT require `CurrentUser` or role check
  - Import: `from dateutil.rrule import rrulestr` (already installed via `icalendar`)
- **Success**:
  - Phase 4 xfail tests now pass
  - `uv run mypy services/api/services/games.py` passes
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 196-210) — full implementation spec
  - #file:../research/20260615-01-recurring-games-research.md (Lines 68-79) — `_persist_and_publish()` pattern (deferred game — what NOT to do)
- **Dependencies**:
  - Task 4.1 complete; Phase 1 complete

### Task 5.2: Remove xfail markers from Phase 4 tests

- **Files**:
  - `tests/unit/services/test_system_clone_for_recurrence.py` — remove all `@pytest.mark.xfail`
- **Success**:
  - `uv run pytest tests/unit/services/test_system_clone_for_recurrence.py -v` — all PASSED
- **Dependencies**:
  - Task 5.1 complete

---

## Phase 6: Handler Modifications Stubs + RED Unit Tests

### Task 6.1: Add `_handle_recurrence_confirmation` stub to `EventHandlers`

Add a `NotImplementedError` stub in `services/bot/events/handlers.py` so Phase 6 tests can import it.

- **Files**:
  - `services/bot/events/handlers.py` — add stub method after `_handle_clone_confirmation`
- **Implementation**:
  ```python
  async def _handle_recurrence_confirmation(self, event: NotificationDueEvent) -> None:
      raise NotImplementedError
  ```
- **Success**:
  - `uv run mypy services/bot/events/handlers.py` passes
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 229-232) — `_handle_recurrence_confirmation` spec
- **Dependencies**:
  - Phase 3 complete (needs `RecurrenceConfirmationView`)

### Task 6.2: Add xfail unit tests for handler modifications

Create `tests/unit/services/bot/events/test_handlers_recurrence.py`.

- **Files**:
  - `tests/unit/services/bot/events/test_handlers_recurrence.py` — new file
- **Tests** (all `@pytest.mark.xfail(strict=True, reason="...")`):

  **`_handle_post_transition_actions` recurrence trigger:**
  - `test_post_transition_triggers_recurrence_clone_when_recur_rule_set` — at COMPLETED with `recur_rule` set, `_system_clone_for_recurrence` is called and a `recurrence_confirmation` notification is scheduled
  - `test_post_transition_skips_recurrence_when_no_recur_rule` — at COMPLETED with `recur_rule=None`, no clone is created

  **`_handle_status_transition_due` zombie check:**
  - `test_in_progress_transition_cancels_unannounced_recurrence` — game with `message_id=None AND recur_rule is not None` transitioning to IN_PROGRESS is cancelled (status=CANCELLED)
  - `test_in_progress_transition_proceeds_for_announced_recurrence` — game with `message_id` set and `recur_rule` set proceeds normally
  - `test_in_progress_transition_proceeds_for_regular_game_no_announce` — game with `message_id=None AND recur_rule=None` proceeds normally (no false positive)

  **`_handle_recurrence_confirmation` dispatch:**
  - `test_handle_recurrence_confirmation_sends_host_dm` — handler sends DM to game host with `RecurrenceConfirmationView`

- **Success**:
  - `uv run pytest tests/unit/services/bot/events/test_handlers_recurrence.py -v` — all `xfailed`
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 233-241, 289-320) — handler specs and test plan
- **Dependencies**:
  - Task 6.1 complete

---

## Phase 7: Handler Modifications GREEN

### Task 7.1: Modify `_handle_post_transition_actions` to trigger recurrence clone

In `services/bot/events/handlers.py`, add recurrence clone trigger inside `_handle_post_transition_actions` after the rewards-reminder block.

- **Files**:
  - `services/bot/events/handlers.py` — modify `_handle_post_transition_actions`
- **Implementation**:
  ```python
  if target_status == GameStatus.COMPLETED.value and game.recur_rule:
      from dateutil.rrule import rrulestr
      next_at = rrulestr(game.recur_rule, dtstart=game.scheduled_at).after(game.scheduled_at)
      if next_at:
          async with get_db_session() as db:
              game_service = GameService(db)
              clone = await game_service._system_clone_for_recurrence(db, game, next_at)
              # Schedule recurrence_confirmation notification for the host
              await _schedule_recurrence_confirmation_notification(db, clone)
              await db.commit()
  ```
  The helper `_schedule_recurrence_confirmation_notification` inserts a `NotificationSchedule` row with `notification_type="recurrence_confirmation"` at `next_at - timedelta(seconds=60)` (or immediately).
- **Success**:
  - Phase 6 xfail tests for `test_post_transition_*` pass
  - `uv run mypy services/bot/events/handlers.py` passes
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 233-241) — handler pseudocode
  - #file:../research/20260615-01-recurring-games-research.md (Lines 75-80) — `rrulestr` usage
- **Dependencies**:
  - Phase 5 complete (`_system_clone_for_recurrence` implemented)

### Task 7.2: Add zombie-clone cancel to `_handle_status_transition_due`

In `services/bot/events/handlers.py`, inject the zombie check before committing the IN_PROGRESS transition.

- **Files**:
  - `services/bot/events/handlers.py` — modify `_handle_status_transition_due` (or `_is_transition_ready`)
- **Implementation** — add early-exit check inside the DB session block, before `_transition_game_status`:
  ```python
  if (
      transition_event.target_status == GameStatus.IN_PROGRESS.value
      and game.message_id is None
      and game.recur_rule is not None
  ):
      await self._cancel_unconfirmed_recurrence(db, game)
      return
  ```
  Add `_cancel_unconfirmed_recurrence(db, game)` as a private method that sets `game.status = GameStatus.CANCELLED.value` and commits.
- **Success**:
  - Phase 6 xfail zombie tests pass
  - `uv run mypy services/bot/events/handlers.py` passes
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 116-124) — zombie-prevention design
- **Dependencies**:
  - Task 7.1 complete (or can be parallel — no code dependency between 7.1 and 7.2)

### Task 7.3: Implement `_handle_recurrence_confirmation` and add dispatch

- **Files**:
  - `services/bot/events/handlers.py` — replace stub with full implementation; add dispatch case in `_handle_notification_due`
- **Implementation** (model on `_handle_clone_confirmation`):
  ```python
  async def _handle_recurrence_confirmation(self, event: NotificationDueEvent) -> None:
      async with get_db_session() as db:
          game = await self._get_game_with_participants(db, str(event.game_id))
          if not game or not game.host:
              return
          next_at_unix = int(game.scheduled_at.timestamp())
          message = DMFormats.recurrence_confirmation(game.title, next_at_unix)
          view = RecurrenceConfirmationView(str(game.id), ...)
          await self._send_dm(game.host.discord_id, message, view=view)
  ```
  Add dispatch in `_handle_notification_due`:
  ```python
  elif notification_event.notification_type == "recurrence_confirmation":
      await self._handle_recurrence_confirmation(notification_event)
  ```
  Add import: `from services.bot.views.recurrence_confirmation_view import RecurrenceConfirmationView`
- **Success**:
  - Phase 6 xfail test for `_handle_recurrence_confirmation` passes
  - `uv run mypy services/bot/events/handlers.py` passes
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 229-232) — spec
  - #file:../research/20260615-01-recurring-games-research.md (Lines 40-47) — `_handle_clone_confirmation` as pattern reference
- **Dependencies**:
  - Phase 3 complete; Task 6.1 complete

### Task 7.4: Remove xfail markers from Phase 6 tests

- **Files**:
  - `tests/unit/services/bot/events/test_handlers_recurrence.py` — remove all `@pytest.mark.xfail`
- **Success**:
  - `uv run pytest tests/unit/services/bot/events/test_handlers_recurrence.py -v` — all PASSED
- **Dependencies**:
  - Tasks 7.1, 7.2, 7.3 complete

---

## Phase 8: Integration Tests (Retrofitting)

### Task 8.1: Fix `update_game` — handle `clear_post_at=true` for `post_at=NULL` recurrence clones

Fix the `clear_post_at` precondition in `update_game()` in `services/api/services/games.py` to also handle recurrence clones where `post_at=NULL AND recur_rule IS NOT NULL`, and write unit tests for the new branch.

- **Files**:
  - `services/api/services/games.py` — widen the `clear_post_at` precondition
  - `tests/unit/services/` — add unit tests for the new branch (extend the existing `update_game` test file)
- **Implementation**:
  Change the precondition from:
  ```python
  if update_data.clear_post_at and game.post_at is not None and game.message_id is None:
  ```
  To:
  ```python
  if update_data.clear_post_at and game.message_id is None and (
      game.post_at is not None or game.recur_rule is not None
  ):
      if game.post_at is None:
          game.post_at = datetime.datetime.now(datetime.UTC).replace(tzinfo=None)
  ```
- **Tests**:
  - `test_clear_post_at_sets_post_at_for_null_post_at_recurrence_clone` — `PUT /{clone_id}` with `clear_post_at=true` on a clone with `post_at=NULL AND recur_rule IS NOT NULL` sets `post_at` to approximately now
  - `test_clear_post_at_is_no_op_for_null_post_at_non_recurrence` — `PUT /{game_id}` with `clear_post_at=true` on a game with `post_at=NULL AND recur_rule=None` does not set `post_at` (unchanged behavior)
- **Success**:
  - New unit tests pass
  - `uv run mypy services/api/services/games.py` passes
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md — Addendum: Problem 3 and Revised Phase 8 Task 8.1
- **Dependencies**:
  - Phase 7 complete

### Task 8.2: Write integration tests for recurring game clone lifecycle

Create `tests/integration/test_recurrence_clone.py`. These test already-implemented code (retrofitting), so no `xfail` is needed.

- **Files**:
  - `tests/integration/test_recurrence_clone.py` — new file
- **Tests** (4 tests — corrected from original 6; bot-dependent scenarios moved to e2e):
  - `test_recur_rule_stored_and_returned` — `POST /api/v1/games` with `recur_rule="FREQ=WEEKLY;BYDAY=SA"` → `GET /api/v1/games/{id}` returns `recur_rule` in response
  - `test_recur_rule_propagated_through_clone_endpoint` — `POST /games/{id}/clone` on a game with `recur_rule`; verify response carries `recur_rule`
  - `test_recurrence_clone_with_null_post_at_is_visible` — directly insert clone with `post_at=NULL`; verify both host AND a regular player can see it via `GET /api/v1/games` (clones with `post_at=NULL` are visible to all API users — only hidden from the announcement loop)
  - `test_clear_post_at_announces_recurrence_clone` — directly insert clone with `post_at=NULL AND recur_rule IS NOT NULL`; call `PUT /{clone_id}` with `clear_post_at=true`; verify `post_at` is now set and `GAME_CREATED` is published to RabbitMQ (requires Task 8.1 fix)
- **Note**: `test_recurrence_clone_invisible_to_players`, `test_recurrence_clone_visible_to_host`, and `test_zombie_clone_cancelled_on_in_progress` from the original plan are removed — they either tested false assumptions (`post_at=NULL` clones are visible to all API users) or required bot EventHandlers which do not run in integration tests (`BOT_SKIP_STARTUP=1`).
- **Success**:
  - `scripts/run-integration-tests.sh tests/integration/test_recurrence_clone.py |& tee output-integration-recurrence.txt` — all PASSED
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md — Addendum: Problem 1, Problem 2, and Revised Phase 8 task structure
- **Dependencies**:
  - Task 8.1 complete; all prior phases complete

---

## Phase 9: E2E Tests (Retrofitting)

### Task 9.1: Write e2e tests for recurring game host confirmation flow

Create `tests/e2e/test_recurring_game.py`. These test already-implemented code (retrofitting), so no `xfail` is needed.

- **Files**:
  - `tests/e2e/test_recurring_game.py` — new file
- **Tests**:
  - `test_recurring_game_host_confirms_via_api`:
    1. Create game with `recur_rule` set, scheduled 2 min from now with 1 min duration
    2. Wait for COMPLETED status via `wait_for_db_condition` (~3 min)
    3. Assert clone exists with `post_at=NULL, message_id=NULL`
    4. Admin calls `PUT /{clone_id}` with `clear_post_at=true`
    5. Wait for clone `message_id` to become non-NULL (`wait_for_game_message_id`, 60 s timeout)
    6. Assert Discord message posted (`message_id` set)
    7. Assert Player A can now see clone in `GET /api/v1/games`
  - `test_recurring_game_zombie_cancelled_when_unconfirmed`:
    1. Create game with `recur_rule` set, scheduled 2 min from now with 1 min duration
    2. Wait for COMPLETED → clone created with `post_at=NULL`
    3. Insert a clone directly in DB with `scheduled_at=2min, recur_rule` set, `post_at=NULL`
    4. Wait for IN_PROGRESS transition to fire on clone
    5. Assert clone `status=CANCELLED` in DB and `message_id=NULL`
- **Timeout decorators**: `@pytest.mark.timeout(360)` for both tests
- **Note**: Discord button path is intentionally omitted from automated e2e — manual verification only
- **Success**:
  - `scripts/run-e2e-tests.sh tests/e2e/test_recurring_game.py |& tee output-e2e-recurrence.txt` — all PASSED
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 326-360) — full e2e test spec and timeout guidance
- **Dependencies**:
  - All prior phases complete

---

## Phase 10: Frontend `RecurrenceSelector` Component

### Task 10.1: Write failing Vitest tests for `RecurrenceSelector`

Follow TDD: write tests first, then implement.

- **Files**:
  - `frontend/src/components/RecurrenceSelector.test.tsx` — new test file
- **Tests** (use `test.fails` or write assertions that will fail before implementation):
  - `renders_no_recurrence_by_default` — dropdown shows "No recurrence" initially
  - `shows_interval_field_when_weekly_selected` — selecting "Every N weeks" reveals a numeric stepper
  - `shows_interval_field_when_monthly_date_selected` — "Every N months on same date" reveals stepper
  - `shows_interval_field_when_monthly_weekday_selected` — "Every N months on same weekday" reveals stepper
  - `computes_weekly_rrule_correctly` — with `scheduledAt` on a Saturday, interval=2 → `"FREQ=WEEKLY;INTERVAL=2;BYDAY=SA"`
  - `computes_monthly_date_rrule_correctly` — with `scheduledAt` on the 15th, interval=1 → `"FREQ=MONTHLY;INTERVAL=1;BYMONTHDAY=15"`
  - `computes_monthly_weekday_rrule_correctly` — with `scheduledAt` on the 1st Friday, interval=3 → `"FREQ=MONTHLY;INTERVAL=3;BYDAY=1FR"`
  - `calls_onChange_with_null_when_no_recurrence_selected` — onChange called with `null` when "No recurrence" selected
- **Success**:
  - `cd frontend && npm run test -- RecurrenceSelector` — tests fail with expected errors (RED)
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 131-163) — component design, RRULE table, ordinal computation
- **Dependencies**:
  - Phase 1 complete (schemas accept `recur_rule`)

### Task 10.2: Implement `RecurrenceSelector` component

Create the component so Phase 10.1 tests pass.

- **Files**:
  - `frontend/src/components/RecurrenceSelector.tsx` — new component
- **Props**:
  ```typescript
  interface Props {
    scheduledAt: Date | null;
    value: string | null; // current RRULE string
    onChange: (rule: string | null) => void;
  }
  ```
- **Implementation**:
  - Top-level `Select` with options: `"none"`, `"weekly"`, `"monthly_date"`, `"monthly_weekday"`
  - When `"none"`: call `onChange(null)`
  - When any other option: show interval `TextField type="number"` (min=1, max=8 for weekly, max=12 for monthly)
  - On change, compute RRULE from `scheduledAt`:
    - `weekly`: `DOW` = `["SU","MO","TU","WE","TH","FR","SA"][scheduledAt.getDay()]`; result `FREQ=WEEKLY;INTERVAL={n};BYDAY={DOW}`
    - `monthly_date`: `DAY` = `scheduledAt.getDate()`; result `FREQ=MONTHLY;INTERVAL={n};BYMONTHDAY={DAY}`
    - `monthly_weekday`: compute ordinal (+1..+4 or -1) and `DOW`; result `FREQ=MONTHLY;INTERVAL={n};BYDAY={ORD}{DOW}`
  - Pattern: follow `DurationSelector` component (same `FormControl` + `Select` + conditional `TextField`)
- **Success**:
  - `cd frontend && npm run test -- RecurrenceSelector` — all tests PASSED
  - `cd frontend && npm run build` passes
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 131-163) — RRULE computation table and ordinal values
- **Dependencies**:
  - Task 10.1 complete

### Task 10.3: Wire `RecurrenceSelector` into the game create/edit form

Integrate the new component into the game creation and edit forms, sending `recur_rule` in the request body.

- **Files**:
  - Game create form component (find via `grep -r "scheduledAt\|scheduled_at" frontend/src --include="*.tsx" -l`)
  - Game edit form component (similar search)
- **Steps**:
  - Import `RecurrenceSelector` into the form
  - Add `recur_rule` state field (initially `null`)
  - Render `<RecurrenceSelector scheduledAt={...} value={recurRule} onChange={setRecurRule} />`
  - Include `recur_rule` in the POST/PUT request body
- **Success**:
  - `cd frontend && npm run build` passes
  - Creating a game via UI with a recurrence rule selected → `GET /api/v1/games/{id}` returns `recur_rule` set
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 127-163) — `RecurrenceSelector` UI design
- **Dependencies**:
  - Task 10.2 complete

---

## Phase 11: Pending Confirmation UI

### Task 11.1: Write RED unit tests for `display_status` computation

Write xfail unit tests for the `display_status` virtual field before any implementation.

- **Files**:
  - `tests/unit/services/api/test_games_display_status.py` — new test file
- **Tests**:
  - `test_display_status_is_pending_confirmation_when_scheduled_with_recur_rule_and_null_post_at_and_null_message_id`
  - `test_display_status_is_scheduled_for_regular_game_without_recur_rule`
  - `test_display_status_is_scheduled_when_post_at_is_set`
  - `test_display_status_is_scheduled_when_message_id_is_set`
  - `test_display_status_passes_through_non_scheduled_statuses` (CANCELLED, IN_PROGRESS, COMPLETED)
- **Implementation**: All tests marked `@pytest.mark.xfail(strict=True)`
- **Success**:
  - `uv run pytest tests/unit/services/api/test_games_display_status.py -v` — all xfail
  - `uv run mypy shared/ services/` — passes
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 452-496) — pending confirmation state definition and `display_status` computation
- **Dependencies**:
  - Phase 10 complete

### Task 11.2: Implement `display_status` in schema + route layer

Add `display_status: str` to `GameResponse` and compute it in the route layer when building responses.

- **Files**:
  - `services/api/schemas/game.py` — add `display_status: str` field to `GameResponse`
  - `services/api/routes/games.py` — compute and set `display_status` when constructing `GameResponse`
- **Implementation**:

  ```python
  # In GameResponse schema:
  display_status: str

  # In route layer (wherever GameResponse is built from a GameSession):
  is_pending_confirmation = (
      game.status == "SCHEDULED"
      and game.recur_rule is not None
      and game.post_at is None
      and game.message_id is None
  )
  display_status = "PENDING_CONFIRMATION" if is_pending_confirmation else game.status
  ```

- **Success**:
  - `uv run pytest tests/unit/services/api/test_games_display_status.py -v` — all PASSED (xfail markers removed)
  - `uv run mypy shared/ services/` — no errors
  - `GET /api/v1/games/{id}` for a clone with `post_at=NULL, message_id=NULL, recur_rule IS NOT NULL` returns `"display_status": "PENDING_CONFIRMATION"`
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 477-496) — backend schema and route computation design
- **Dependencies**:
  - Task 11.1 complete

### Task 11.3: Update frontend `GameSession` type + `GameCard` chip and color

Add `display_status` to the TypeScript type and update `GameCard` to use it for chip label and color.

- **Files**:
  - `frontend/src/types/index.ts` — add `display_status?: string` to `GameSession` interface
  - `frontend/src/components/GameCard.tsx` — use `display_status`, add `"PENDING_CONFIRMATION"` → `"warning"` color mapping
- **Implementation**:
  - Add `display_status?: string` to `GameSession` in `frontend/src/types/index.ts`
  - In `getStatusColor()` (or equivalent color mapping): `case "PENDING_CONFIRMATION": return "warning"`
  - In chip label: replace `game.status` with `game.display_status ?? game.status`
  - The "Pending posting" chip (`post_at && !message_id`) is unaffected — different condition
- **Tests**:
  - Vitest test: `GameCard` with `display_status="PENDING_CONFIRMATION"` renders warning-colored chip
  - Vitest test: `GameCard` with no `display_status` falls back to `status` field
- **Success**:
  - `cd frontend && npm run test -- --reporter=verbose GameCard` — all tests pass
  - `cd frontend && npm run build` — no TypeScript errors
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 497-511) — frontend type and `GameCard` chip design
- **Dependencies**:
  - Task 11.2 complete

### Task 11.4: Update `GameDetails` with pending confirmation alert

Show an informational MUI `Alert` in `GameDetails` when `display_status === "PENDING_CONFIRMATION"` and the viewer has edit access.

- **Files**:
  - `frontend/src/pages/GameDetails.tsx` — update status chip to use `display_status`; add `Alert` for pending confirmation
- **Implementation**:
  - Status chip: use `game.display_status ?? game.status` for label and color (same pattern as `GameCard`)
  - When `display_status === "PENDING_CONFIRMATION"` and `canEdit`: render MUI `Alert` with `severity="info"`:
    _"This recurring game is awaiting confirmation. Check your Discord DMs to confirm or cancel the next occurrence."_
  - No Confirm button on the web UI — host must use Discord DM (rationale: no stored DM message ID to delete after confirmation)
  - Existing Cancel Game button and dialog remain unchanged
- **Tests**:
  - Vitest test: when `display_status="PENDING_CONFIRMATION"` and user can edit → `Alert` is rendered
  - Vitest test: when `display_status="PENDING_CONFIRMATION"` and user cannot edit → no `Alert`
  - Vitest test: when `display_status="SCHEDULED"` → no `Alert`
- **Success**:
  - `cd frontend && npm run test -- --reporter=verbose GameDetails` — all tests pass
  - `cd frontend && npm run build` — no TypeScript errors
- **Research References**:
  - #file:../research/20260615-01-recurring-games-research.md (Lines 512-533) — `GameDetails` alert design and rationale for no web Confirm button
- **Dependencies**:
  - Task 11.3 complete

---

## Dependencies

- `python-dateutil` ≥ 2.9.0 (already installed as transitive dep of `icalendar~=6.0.0`)
- No new pip packages required

## Success Criteria

- Game with `recur_rule` set completes → clone exists with `post_at=NULL, message_id=NULL`
- Host confirms via Discord button → clone announced immediately
- Host confirms via `PUT /{clone_id}` with `clear_post_at=true` → clone announced (API/e2e path)
- Host ignores → clone cancelled when IN_PROGRESS transition fires
- Host declines via Discord → clone cancelled immediately
- Clone inherits `recur_rule` → recurrence chain continues
- Games without `recur_rule` → no change to existing behavior
- All unit, integration, and e2e tests pass
