import { FC } from 'react';
import { Typography } from '@mui/material';

export const BrowseGames: FC = () => {
  return (
    <div>
      <Typography variant="h4">Browse Games</Typography>
      <Typography>Coming soon...</Typography>
    </div>
  );
};
